fragment BasePageMediaFields on PageTopic {
  __typename
  sys {
    id
    spaceId
  }
  internalName
  slug
  seoFields {
    ...SeoFields
  }
  name
  description
  featuredMediaImage {
    ...ImageFields
  }
}

fragment PageMediaFields on PageTopic {
  ...BasePageMediaFields
}

query pageMedia($slug: String!, $locale: String, $preview: Boolean) {
  
}
